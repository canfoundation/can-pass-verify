# Realease

Make sure you are checked out master branch

```bash
# Make sure you are checked out master branch
# or `git checkout master`
git pull
yarn release
git push --follow-tags origin master
```
